This directory is mapped to the directory enabled S3 bucket

The API gateway will have a mapping /secure/
This mapping will require authentication (registered users must be logged in)

Anything in this directory on the local disk is not really testable
- the security of the items isn't testable
- basic layout and URL mapping =MAY= be testable


